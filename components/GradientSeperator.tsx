export default function GradientSeperator({first, second}:{
    first: string;
    second: string;
}) {

    return (
        <div className={`bg-gradient-to-b from-[#ffbedb] from-opa to-[#00bcd4] h-[30vh] w-full`} />
    )
}